export type Register = {
  email: string;
  password: string;
  name: string;
  phone: string;
  nationality: string;
  role_id: number;
};

export type AlertMessage = {
  title: string;
  content: string;
};
